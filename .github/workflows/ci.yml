name: ci

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  static:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run static tests
        shell: pwsh
        run: ./scripts/test.ps1
